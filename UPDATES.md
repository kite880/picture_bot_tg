# Обновления - История отправок 🎉

## Что изменилось

### 1. Постоянная история отправок ✅

Теперь бот **сохраняет все отправленные картинки** в файл `sent_history.json`. Это гарантирует, что картинки никогда не повторяются.

**Раньше:**
- Картинки не повторялись только в течение дня
- В полночь счетчик сбрасывался
- На следующий день можно было отправить те же картинки

**Теперь:**
- Все отправленные картинки запоминаются
- Картинки никогда не повторяются
- История сохраняется в файл для надежности

### 2. Команды управления бота 🤖

Добавлены команды для взаимодействия с ботом:

| Команда | Описание |
|---------|---------|
| `/start` | Главное меню и список команд |
| `/stats` | Показать статистику отправок |
| `/reset_history` | Сбросить историю (очистить все данные) |
| `/help` | Справка по использованию |

**Пример статистики:**
```
📊 Статистика:

Всего картинок в папке: 100
Отправлено картинок: 42
Осталось неотправленных: 58
Прогресс: 42.0%
```

### 3. Новый файл `history.py`

Создан отдельный модуль для управления историей:

**Основные функции:**
- `add_image()` - добавить картинку в историю
- `is_sent()` - проверить, отправлена ли картинка
- `get_unsent_images()` - получить список неотправленных картинок
- `reset_history()` - очистить историю
- `get_stats()` - получить статистику

**Пример использования:**
```python
from history import HistoryManager

history = HistoryManager()

# Добавить картинку в историю
history.add_image("/path/to/image.jpg")

# Проверить отправлена ли картинка
if history.is_sent("/path/to/image.jpg"):
    print("Картинка уже отправлена")

# Получить неотправленные картинки
unsent = history.get_unsent_images(all_images)

# Получить статистику
stats = history.get_stats()
print(f"Отправлено: {stats['total_sent']}")
```

### 4. Файл истории `sent_history.json`

Новый файл для сохранения данных об отправках:

```json
{
  "images": [
    "/home/user/pictures/image1.jpg",
    "/home/user/pictures/image2.png",
    ...
  ],
  "last_updated": "2024-11-20T18:30:45.123456",
  "total_count": 42
}
```

**Особенности:**
- Автоматически создается при первой отправке
- Обновляется после каждой отправки
- Можно отредактировать вручную
- НЕ коммитится в Git (в .gitignore)

### 5. Обновленный `main.py`

**Основные изменения:**

1. **Интеграция с HistoryManager**
   - При старте загружается история
   - При отправке картинка добавляется в историю
   - При выборе картинки исключаются отправленные

2. **Команды Telegram**
   - Добавлена обработка команд с помощью `CommandHandler`
   - Функции обработки команд используют async/await
   - Ответы отправляются через `update.message.reply_text()`

3. **Асинхронная архитектура**
   - Использован `Application` из `python-telegram-bot`
   - Scheduler работает в отдельном потоке
   - Главный поток обрабатывает команды

4. **Логирование**
   - Добавлены дополнительные логи о работе истории
   - Улучшены сообщения об ошибках

## Как использовать

### Базовое использование

```bash
python main.py
```

Бот автоматически:
1. Загружает историю из файла
2. Отправляет картинки по расписанию
3. Сохраняет историю после каждой отправки
4. Обрабатывает команды пользователя

### Проверка статистики

Отправьте в чат команду:
```
/stats
```

Получите ответ:
```
📊 Статистика:

Всего картинок в папке: 100
Отправлено картинок: 42
Осталось неотправленных: 58
Прогресс: 42.0%
```

### Сброс истории

Отправьте в чат команду:
```
/reset_history
```

Бот очистит историю и подтвердит:
```
🔄 История сброшена!
Удалено 42 записей о отправленных картинках.
Теперь все картинки считаются новыми.
```

## Новые файлы

```
picture_bot_tg/
├── history.py                 # NEW: Модуль управления историей
├── sent_history.json          # NEW: Файл истории (создается автоматически)
├── HISTORY_FEATURE.md         # NEW: Документация по функции истории
├── UPDATES.md                 # NEW: Этот файл
└── ...остальные файлы
```

## Миграция со старой версии

Если вы использовали старую версию без истории:

1. **Вообще ничего делать не нужно!** ✅
2. Просто обновите `main.py` и добавьте `history.py`
3. Новая история создастся автоматически при первой отправке
4. Старые картинки будут считаться новыми

## Технические детали

### Как сохраняется история

1. При загрузке приложения вызывается `HistoryManager()`
2. Конструктор загружает данные из `sent_history.json`
3. После отправки картинки вызывается `history_manager.add_image(path)`
4. Функция `add_image()` добавляет путь и сохраняет JSON

### Как выбираются картинки

```python
# Получить неотправленные картинки
unsent = history_manager.get_unsent_images(all_images)

# Выбрать случайную
selected = random.choice(unsent)

# Отправить
send_image(selected)

# Добавить в историю
history_manager.add_image(selected)
```

### Асинхронный scheduler

```
Main Thread                    Background Thread
│                             │
├─ Initialize Application      │
├─ Setup Command Handlers      │
├─ Configure Scheduler         │
│                             │
├─ Start Scheduler Thread ────┼──> scheduler_loop()
│                             │    ├─ check every 60s
│                             │    ├─ run pending tasks
│                             │    └─ sleep 60s
│                             │
├─ Run Polling for Commands    │
│  (handles /stats, /reset...)  │
│                             │
└─ Wait for user input        └─> Background jobs
```

## Возможные проблемы и решения

### История не сохраняется
- Проверьте права доступа: `chmod 755 /path/to/bot`
- Убедитесь, что папка не защищена от записи

### Файл истории поврежден
- Удалите `sent_history.json`
- Бот создаст новый файл при следующей отправке
- Все картинки снова будут считаться новыми

### Слишком много картинок отправлено
- Сброс истории: `/reset_history`
- Или отредактируйте `sent_history.json` вручную

## Обратная совместимость

✅ **Полная обратная совместимость** с конфигурацией:
- Все переменные из `.env` работают как раньше
- SEND_INTERVAL, START_HOUR, END_HOUR - все то же самое
- Картинки хранятся в IMAGES_PATH - все то же самое

## Что дальше?

Возможные улучшения в будущем:
- [ ] Веб-интерфейс для просмотра истории
- [ ] Экспорт истории в CSV/Excel
- [ ] Фильтрация картинок по датам
- [ ] Статистика по дням и неделям
- [ ] Интеграция с базой данных

---

**Спасибо за использование Picture Bot!** 🎊

Если у вас есть вопросы, смотрите:
- `HISTORY_FEATURE.md` - детали функции истории
- `QUICKSTART.md` - быстрый старт
- `README.md` - полная документация
