# Структура проекта 📁

```
picture_bot_tg/
├── main.py                 # Основной файл бота
├── config.py              # Конфигурация и валидация
├── requirements.txt       # Зависимости Python
├── .env                   # Файл конфигурации (заполнить вручную)
├── .gitignore            # Git игнор файлы
├── start.sh              # Скрипт запуска (Linux/Mac)
├── start.bat             # Скрипт запуска (Windows)
├── check_config.py       # Проверка конфигурации
├── get_chat_id.py        # Получение Chat ID
├── README.md             # Подробная документация
├── QUICKSTART.md         # Быстрый старт
└── STRUCTURE.md          # Этот файл
```

## Описание файлов

### Основные файлы

**main.py**
- Основной код Telegram бота
- Загружает картинки из папки
- Отправляет картинки по расписанию
- Гарантирует отсутствие дублей в течение дня
- Использует schedule для планирования

**config.py**
- Читает конфигурацию из .env
- Валидирует все параметры
- Предоставляет Config класс для доступа к параметрам
- Может быть запущен отдельно для проверки конфигурации

**requirements.txt**
- Список всех Python зависимостей
- Содержит версии пакетов

**.env**
- Хранит чувствительные данные (токены)
- Содержит пути к папкам
- Содержит параметры расписания
- **НЕ КОММИТИТСЯ в Git** (.gitignore защищает)

### Вспомогательные скрипты

**check_config.py**
- Проверяет валидность конфигурации
- Показывает текущие настройки
- Рассчитывает количество отправок в день
- Используется для быстрой диагностики

**get_chat_id.py**
- Помощник для получения Chat ID
- Выдаёт инструкции пошагово
- Помогает при первоначальной настройке

**start.sh** (Linux/Mac)
- Автоматический запуск бота
- Создаёт виртуальное окружение
- Устанавливает зависимости
- Проверяет конфигурацию
- Запускает бота

**start.bat** (Windows)
- Windows версия start.sh
- Аналогичный функционал
- Работает из командной строки

### Документация

**README.md**
- Подробное описание проекта
- Полная документация
- Примеры конфигураций
- Решение проблем

**QUICKSTART.md**
- Быстрый старт за 5 минут
- Пошаговые инструкции
- Примеры конфигураций

**.gitignore**
- Исключает .env (не коммитим токены!)
- Исключает __pycache__ и *.pyc
- Исключает venv/
- Исключает IDE файлы

## Зависимости

```
python-telegram-bot==20.7  # Telegram Bot API
python-dotenv==1.0.0       # Загрузка .env файлов
schedule==1.2.0            # Планирование задач
```

## Как всё работает

1. **Инициализация**
   - Загружаются переменные окружения из .env
   - Создаётся объект Bot с токеном
   - Загружаются все картинки из папки

2. **Расписание**
   - Создаются задачи для каждого часа в диапазоне START_HOUR..END_HOUR
   - Интервал между отправками = SEND_INTERVAL
   - Например: 9:00, 11:00, 13:00, 15:00, 17:00, 19:00

3. **Отправка**
   - Бот выбирает случайную картинку, которая ещё не отправлялась сегодня
   - Отправляет её в Telegram
   - Помечает как отправленную

4. **Сброс**
   - В 00:00 каждого дня список отправленных картинок сбрасывается
   - Картинки можно отправлять снова

## Запуск в фоне

### Linux/Mac
```bash
nohup python3 main.py > bot.log 2>&1 &
```

### Windows (как служба)
1. Установите `nssm` (Non-Sucking Service Manager)
2. Откройте cmd от администратора
3. `nssm install PictureBot "C:\path\to\python\python.exe" "C:\path\to\main.py"`

---

**Кол-во строк кода:** ~300
**Требуемые навыки:** Python базовый уровень
**Время установки:** 5 минут
