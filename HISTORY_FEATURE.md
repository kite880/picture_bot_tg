# Функция истории отправок 📜

## Как это работает

Бот сохраняет всю историю отправленных картинок в файл `sent_history.json`. Это гарантирует, что картинки **никогда не будут повторяться**.

### Логика работы

1. **Загрузка истории** - при запуске бот загружает список всех ранее отправленных картинок
2. **Выбор картинок** - перед отправкой бот выбирает только из картинок, которых нет в истории
3. **Сохранение истории** - после успешной отправки картинка добавляется в историю
4. **Автоматическое сохранение** - история автоматически сохраняется в JSON файл после каждой отправки

### Файл истории

Файл `sent_history.json` содержит:
```json
{
  "images": [
    "/path/to/image1.jpg",
    "/path/to/image2.png",
    ...
  ],
  "last_updated": "2024-11-20T18:30:45.123456",
  "total_count": 42
}
```

## Команды для управления историей

### /stats - Показать статистику

Показывает:
- Сколько всего картинок в папке
- Сколько картинок уже отправлено
- Сколько картинок осталось отправить
- Процент прогресса

Пример:
```
📊 Статистика:

Всего картинок в папке: 100
Отправлено картинок: 42
Осталось неотправленных: 58
Прогресс: 42.0%
```

### /reset_history - Сбросить историю

Полностью очищает историю отправленных картинок. После этого:
- Все картинки снова считаются новыми
- Бот начнет заново отправлять с начала
- Старый файл истории перезаписывается

Пример:
```
🔄 История сброшена!
Удалено 42 записей о отправленных картинках.
Теперь все картинки считаются новыми.
```

## Другие команды

### /start - Главное меню

Показывает приветствие и список доступных команд.

### /help - Справка

Показывает информацию о том, как работает бот и его команды.

## Сценарии использования

### Сценарий 1: Все картинки отправлены

Когда все картинки из папки отправлены:

```
⚠️ Все картинки отправлены! Осталось 0 неотправленных.
```

**Варианты действий:**
1. Добавьте новые картинки в папку
2. Используйте команду `/reset_history` для повторной отправки

### Сценарий 2: Добавление новых картинок

Если вы добавили новые картинки в папку:

1. Бот автоматически найдет их (достаточно перезагрузить папку)
2. Новые картинки будут отправлены в очередной раз
3. Старые картинки не будут отправлены повторно

### Сценарий 3: Полный сброс

Если вы хотите начать сначала:

1. Используйте команду `/reset_history`
2. Все картинки станут новыми
3. История будет стерта

## Советы и трюки

### Просмотр истории вручную

Вы можете открыть файл `sent_history.json` в текстовом редакторе, чтобы:
- Увидеть список всех отправленных картинок
- Проверить когда в последний раз была обновлена история

### Удаление конкретной картинки из истории

Если нужно повторно отправить одну конкретную картинку:

1. Откройте `sent_history.json`
2. Найдите строку с путем к картинке
3. Удалите эту строку из массива `images`
4. Сохраните файл

После этого картинка будет отправлена снова при следующей отправке.

### Бекап истории

Вы можете сделать бекап истории, скопировав файл `sent_history.json`:

```bash
cp sent_history.json sent_history.json.backup
```

## Диагностика проблем

### "All images have been sent"

Это означает, что все картинки в папке уже были отправлены.

**Решение:**
- Добавьте новые картинки в папку
- Или используйте `/reset_history` для повторной отправки

### История не сохраняется

Проверьте права доступа к папке. Бот должен иметь право на запись в папке.

```bash
# Linux/Mac
chmod 755 /path/to/bot/folder
```

### Потеря истории

Если файл `sent_history.json` был удален:
- История восстановить нельзя
- Бот создаст новый файл при следующей отправке
- Все картинки снова считаются новыми

---

**Эта функция гарантирует, что ваши подписчики не будут получать одинаковые картинки!** 🎉
