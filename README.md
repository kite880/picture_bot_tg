# Picture Bot - Telegram Bot для отправки картинок

Telegram бот, который отправляет картинки в установленное время без повторений.

## Требования

- Python 3.8+
- Telegram Bot Token
- Папка с картинками

## Установка

1. **Клонируйте или скачайте проект:**
```bash
cd picture_bot_tg
```

2. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

3. **Создайте файл `.env`** (уже создан, нужно заполнить):
```bash
# Получите токен у @BotFather в Telegram
BOT_TOKEN=YOUR_BOT_TOKEN_HERE

# Укажите путь к папке с картинками
IMAGES_PATH=/path/to/your/images/folder

# Интервал отправки в часах (по умолчанию 2)
SEND_INTERVAL=2

# Время начала (часов, 24-часовой формат)
START_HOUR=9

# Время окончания
END_HOUR=21

# ID чата для отправки (см. инструкцию ниже)
CHAT_ID=123456789
```

## Получение CHAT_ID

1. Создайте бота через @BotFather в Telegram
2. Отправьте любое сообщение боту
3. Откройте в браузере:
```
https://api.telegram.org/bot{YOUR_BOT_TOKEN}/getUpdates
```
Замените `{YOUR_BOT_TOKEN}` на ваш токен

4. В ответе найдите `chat` → `id`:
```json
{
  "ok": true,
  "result": [
    {
      "message": {
        "chat": {
          "id": 123456789,  <-- Это ваш CHAT_ID
          ...
        }
      }
    }
  ]
}
```

5. Добавьте этот ID в файл `.env`

## Использование

```bash
python main.py
```

## Как работает

- **Расписание:** Бот отправляет картинки каждые N часов в промежутке от START_HOUR до END_HOUR
- **Без повторений:** Каждый день картинки не повторяются (отправляются только новые)
- **Сброс:** В 00:00 список отправленных картинок сбрасывается, и можно отправлять уже отправленные картинки снова

## Примеры конфигурации

### Отправка каждые 2 часа с 9:00 до 21:00 (6 картинок в день)
```
SEND_INTERVAL=2
START_HOUR=9
END_HOUR=21
```

### Отправка каждые 3 часа с 8:00 до 20:00 (4-5 картинок в день)
```
SEND_INTERVAL=3
START_HOUR=8
END_HOUR=20
```

### Отправка каждый час с 10:00 до 18:00 (8-9 картинок в день)
```
SEND_INTERVAL=1
START_HOUR=10
END_HOUR=18
```

## Логирование

Все события записываются в консоль:
- Загрузка картинок
- Отправка сообщений
- Ошибки и предупреждения

## Структура проекта

```
picture_bot_tg/
├── main.py           # Основной код бота
├── requirements.txt   # Зависимости Python
├── .env              # Конфигурация (заполните вручную)
└── README.md         # Этот файл
```

## Поддерживаемые форматы изображений

- JPG / JPEG
- PNG
- GIF
- BMP
- WebP

## Возможные проблемы

### "Images path not found"
Убедитесь, что путь в `IMAGES_PATH` существует и указан корректно.

### "No images found in ..."
Проверьте, что в папке действительно есть картинки с поддерживаемыми расширениями.

### "BOT_TOKEN not configured"
Получите токен у @BotFather и добавьте в `.env`

### "Failed to connect to Telegram"
Проверьте интернет-соединение и корректность токена.

## Запуск в фоне (Linux/Mac)

```bash
nohup python main.py &
```

или через screen:

```bash
screen -S picture_bot
python main.py
# Нажмите Ctrl+A, потом D чтобы выйти
```

## Остановка

Нажмите `Ctrl+C` в терминале или убейте процесс:
```bash
pkill -f "python main.py"
```

---

**Наслаждайтесь вашим Telegram ботом!** 🤖
